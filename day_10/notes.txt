CSS

[style attrib] --- [id] --- [class, psuedo-class, attrib] --- [elements]
most specificity value    --------------->        least specificity value

!important will force a specific style on an element. Use only in rare occassions.

mobile first
  <meta name="viewport" content="width=device-width, initial-scale=1">

bootstrap
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">

normalize
  normalize.css is included in bootstrap already. use it if you don't want to use bootstrap.
  sets a default standard (clean-slate) for your browser to avoid compatibility problems.

>>bootstrap
Add the following line in Gemfile:
gem "bootstrap-sass"
$ bundle install

Add the file in app/assets/stylesheets/
bootstrap_and_overrides.scss

Add this line in the file above
@import "bootstrap";

For mobile and desktop compatibility
12 = 100%, 6 = 50%
<div class="col-md-6 col-xs-12"></div>
